import React from 'react';
import FormationStage from '../components/FormationStage';

const Postulado: React.FC = () => <FormationStage title="Postulado" stageKey="POSTULADO" />;

export default Postulado;
