import React from 'react';
import FormationStage from '../components/FormationStage';

const Filosofia: React.FC = () => <FormationStage title="Filosofia" stageKey="FILOSOFIA_DETAIL" />;

export default Filosofia;
