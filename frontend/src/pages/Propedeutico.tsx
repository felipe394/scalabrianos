import React from 'react';
import FormationStage from '../components/FormationStage';

const Propedeutico: React.FC = () => <FormationStage title="Propedêutico" stageKey="PROPEDEUTICO_DETAIL" />;

export default Propedeutico;
